# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET        /api/geoLoc/citationCountByHexagon/:radius/:year        ch.fram.medlineGeo.explore.CitationLocatedController.countByHexagon(radius:Double, year:Int)
GET        /api/countries/citationCount/:year                      ch.fram.medlineGeo.explore.CountryCountController.countByYear(year:Int)
GET        /api/countryPairs/citationCount/:year                   ch.fram.medlineGeo.explore.CountryPairCountController.countByYear(year:Int)

# Home page

# Map static resources from the /public folder to the /assets URL path
